


const firstName = "Mohammed";
const lastName = "Imran";


const age = 21;

let val;

val = firstName + " " + lastName

val = 'immu';

val += 'coder'

val = firstName.length

val = firstName.concat(" " + lastName)

val = firstName.toUpperCase()
val = firstName.toLowerCase()

val = firstName[1]

val = firstName.charAt(6)


const para = 'Hello , this is immucoder, web dev '
val = para.split(",")

val = para.replace("this", "that")

val = para.includes("")


const name = 'mohammed'
val = name.slice(4)
val = name.slice(0, 4)
val = name.slice(0, 4)


console.log(val)


console.log(val)